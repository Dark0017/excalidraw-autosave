{
  "name": "excalidraw-autosave-server",
  "version": "1.0.0",
  "description": "server where states autosaves",
  "main": "server.js",
  "scripts": {
    "dev": "concurrently \"npm start\" \"npm start --prefix=client\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Kshitiz Singh",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^6.2.0",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "multer": "^1.4.2"
  }
}
